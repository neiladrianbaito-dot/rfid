export function AuthCheckingScreen({ isDark, slowHint }: { isDark: boolean; slowHint: boolean }) {
  return <div className={`min-h-screen flex items-center justify-center ${isDark ? "bg-[#020617] text-slate-100" : "bg-slate-50 text-slate-800"}`}><div className="flex flex-col items-center gap-4 px-6 text-center"><img src="/calbayog.png" alt="Calbayog" className="h-14 w-14 rounded-xl object-contain animate-pulse" /><div className="flex items-center gap-2">{[0,150,300].map((delay)=><div key={delay} className={`h-2 w-2 rounded-full animate-bounce ${isDark ? "bg-emerald-400" : "bg-emerald-500"}`} style={{animationDelay:`${delay}ms`}} />)}</div><p className={`text-xs font-semibold ${isDark ? "text-slate-400" : "text-slate-500"}`}>Loading your account…</p>{slowHint&&<p className={`text-[11px] max-w-xs leading-snug ${isDark ? "text-slate-600" : "text-slate-400"}`}>The server may be waking up from a cold start — this can take up to a minute on the first load.</p>}</div></div>;
}
