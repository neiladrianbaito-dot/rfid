import { useEffect, useState } from "react";
import { useLocation } from "wouter";
import { cleanCardUid, getSignedInUser, USER_AUTH_TOKEN_KEY } from "@/lib/api";
export function useDashboardAuth(){const[,setLocation]=useLocation();const[cardUid,setCardUid]=useState("");const[authProfile,setAuthProfile]=useState<{fullName:string;email:string}|null>(null);const[authChecking,setAuthChecking]=useState(true);useEffect(()=>{const token=window.localStorage.getItem(USER_AUTH_TOKEN_KEY);if(!token){setLocation("/signin");setAuthChecking(false);return;}void(async()=>{try{const profile=await getSignedInUser();setAuthProfile({fullName:profile?.user?.fullName||"",email:profile?.user?.email||""});const linkedUid=cleanCardUid(profile?.user?.linkedCardUid||"");if(linkedUid)setCardUid(linkedUid);}catch{window.localStorage.removeItem(USER_AUTH_TOKEN_KEY);setLocation("/signin");}finally{setAuthChecking(false);}})();},[setLocation]);return{cardUid,setCardUid,authProfile,authChecking};}
